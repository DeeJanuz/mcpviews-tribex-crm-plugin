# Enhancements

- Add package/release zip script after registry hosting details are finalized.
- Replace local trusted org/user settings with hosted shared-auth context when the plugin is deployed.
- Add renderer-side stage drag ordering once MCPViews interaction expectations are validated.
- Add direct email campaign launcher handoff when CRM audience rows need a one-click campaign path.
